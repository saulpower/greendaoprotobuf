package com.saulpower.GreenWireTest.database;

import java.util.List;
import de.greenrobot.dao.sync.GreenSync;
import com.google.gson.reflect.TypeToken;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import com.saulpower.GreenWireTest.database.ApplicantChild;
import com.saulpower.GreenWireTest.database.Gender;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table APPLICANT_CHILD.
*/
public class ApplicantChildDao extends AbstractDao<ApplicantChild, Long> {

    public static final String TABLENAME = "APPLICANT_CHILD";

    /**
     * Properties of entity ApplicantChild.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property ExternalID = new Property(0, String.class, "externalID", false, "EXTERNAL_ID");
        public final static Property Guid = new Property(1, String.class, "guid", false, "GUID");
        public final static Property Name = new Property(2, String.class, "name", false, "NAME");
        public final static Property BirthDate = new Property(3, String.class, "birthDate", false, "BIRTH_DATE");
        public final static Property TagString = new Property(4, String.class, "tagString", false, "TAG_STRING");
        public final static Property TenantID = new Property(5, Long.class, "tenantID", false, "TENANT_ID");
        public final static Property SaveResultSaveResultId = new Property(6, long.class, "saveResultSaveResultId", false, "SAVE_RESULT_SAVE_RESULT_ID");
        public final static Property ChildrenApplicationId = new Property(7, long.class, "childrenApplicationId", false, "CHILDREN_APPLICATION_ID");
        public final static Property DateLastModified = new Property(8, String.class, "dateLastModified", false, "DATE_LAST_MODIFIED");
        public final static Property SyncBaseId = new Property(9, Long.class, "syncBaseId", false, "SYNC_BASE_ID");
        public final static Property IsDeleted = new Property(10, Boolean.class, "isDeleted", false, "IS_DELETED");
        public final static Property Version = new Property(11, Integer.class, "version", false, "VERSION");
        public final static Property ApplicationApplicationId = new Property(12, long.class, "applicationApplicationId", false, "APPLICATION_APPLICATION_ID");
        public final static Property Gender = new Property(13, Gender.class, "gender", false, "GENDER");
        public final static Property Id = new Property(14, Long.class, "id", true, "_id");
        public final static Property DateCreated = new Property(15, String.class, "dateCreated", false, "DATE_CREATED");
        public final static Property Notes = new Property(16, String.class, "notes", false, "NOTES");
        public final static Property ApplicationID = new Property(17, String.class, "applicationID", false, "APPLICATION_ID");
    };

    private DaoSession daoSession;

    private Query<ApplicantChild> application_ChildrenQuery;

    public ApplicantChildDao(DaoConfig config) {
        super(config);
    }
    
    public ApplicantChildDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'APPLICANT_CHILD' (" + //
                "'EXTERNAL_ID' TEXT," + // 0: externalID
                "'GUID' TEXT," + // 1: guid
                "'NAME' TEXT," + // 2: name
                "'BIRTH_DATE' TEXT," + // 3: birthDate
                "'TAG_STRING' TEXT," + // 4: tagString
                "'TENANT_ID' INTEGER," + // 5: tenantID
                "'SAVE_RESULT_SAVE_RESULT_ID' INTEGER NOT NULL ," + // 6: saveResultSaveResultId
                "'CHILDREN_APPLICATION_ID' INTEGER NOT NULL ," + // 7: childrenApplicationId
                "'DATE_LAST_MODIFIED' TEXT," + // 8: dateLastModified
                "'SYNC_BASE_ID' INTEGER REFERENCES 'SYNC_BASE'('SYNC_BASE_ID') ," + // 9: syncBaseId
                "'IS_DELETED' INTEGER," + // 10: isDeleted
                "'VERSION' INTEGER," + // 11: version
                "'APPLICATION_APPLICATION_ID' INTEGER NOT NULL ," + // 12: applicationApplicationId
                "'GENDER' INTEGER," + // 13: gender
                "'_id' INTEGER PRIMARY KEY ," + // 14: id
                "'DATE_CREATED' TEXT," + // 15: dateCreated
                "'NOTES' TEXT," + // 16: notes
                "'APPLICATION_ID' TEXT);"); // 17: applicationID
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'APPLICANT_CHILD'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ApplicantChild entity) {
        stmt.clearBindings();
 
        String externalID = entity.getExternalID();
        if (externalID != null) {
            stmt.bindString(1, externalID);
        }
 
        String guid = entity.getGuid();
        if (guid != null) {
            stmt.bindString(2, guid);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(3, name);
        }
 
        String birthDate = entity.getBirthDate();
        if (birthDate != null) {
            stmt.bindString(4, birthDate);
        }
 
        String tagString = entity.getTagString();
        if (tagString != null) {
            stmt.bindString(5, tagString);
        }
 
        Long tenantID = entity.getTenantID();
        if (tenantID != null) {
            stmt.bindLong(6, tenantID);
        }
        stmt.bindLong(7, entity.getSaveResultSaveResultId());
        stmt.bindLong(8, entity.getChildrenApplicationId());
 
        String dateLastModified = entity.getDateLastModified();
        if (dateLastModified != null) {
            stmt.bindString(9, dateLastModified);
        }
 
        Long syncBaseId = entity.getSyncBaseId();
        if (syncBaseId != null) {
            stmt.bindLong(10, syncBaseId);
        }
 
        Boolean isDeleted = entity.getIsDeleted();
        if (isDeleted != null) {
            stmt.bindLong(11, isDeleted ? 1l: 0l);
        }
 
        Integer version = entity.getVersion();
        if (version != null) {
            stmt.bindLong(12, version);
        }
        stmt.bindLong(13, entity.getApplicationApplicationId());
 
        Gender gender = entity.getGender();
        if (gender != null) {
            stmt.bindLong(14, gender.getValue());
        }
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(15, id);
        }
 
        String dateCreated = entity.getDateCreated();
        if (dateCreated != null) {
            stmt.bindString(16, dateCreated);
        }
 
        String notes = entity.getNotes();
        if (notes != null) {
            stmt.bindString(17, notes);
        }
 
        String applicationID = entity.getApplicationID();
        if (applicationID != null) {
            stmt.bindString(18, applicationID);
        }
    }

    @Override
    protected void attachEntity(ApplicantChild entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 14) ? null : cursor.getLong(offset + 14);
    }    

    /** @inheritdoc */
    @Override
    public ApplicantChild readEntity(Cursor cursor, int offset) {
        ApplicantChild entity = new ApplicantChild( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // externalID
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // guid
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // name
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // birthDate
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // tagString
            cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5), // tenantID
            cursor.getLong(offset + 6), // saveResultSaveResultId
            cursor.getLong(offset + 7), // childrenApplicationId
            cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8), // dateLastModified
            cursor.isNull(offset + 9) ? null : cursor.getLong(offset + 9), // syncBaseId
            cursor.isNull(offset + 10) ? null : cursor.getShort(offset + 10) != 0, // isDeleted
            cursor.isNull(offset + 11) ? null : cursor.getInt(offset + 11), // version
            cursor.getLong(offset + 12), // applicationApplicationId
            cursor.isNull(offset + 13) ? null : Gender.fromInt(cursor.getLong(offset + 13)), // gender
            cursor.isNull(offset + 14) ? null : cursor.getLong(offset + 14), // id
            cursor.isNull(offset + 15) ? null : cursor.getString(offset + 15), // dateCreated
            cursor.isNull(offset + 16) ? null : cursor.getString(offset + 16), // notes
            cursor.isNull(offset + 17) ? null : cursor.getString(offset + 17) // applicationID
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ApplicantChild entity, int offset) {
        entity.setExternalID(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setGuid(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setName(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setBirthDate(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setTagString(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setTenantID(cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5));
        entity.setSaveResultSaveResultId(cursor.getLong(offset + 6));
        entity.setChildrenApplicationId(cursor.getLong(offset + 7));
        entity.setDateLastModified(cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8));
        entity.setSyncBaseId(cursor.isNull(offset + 9) ? null : cursor.getLong(offset + 9));
        entity.setIsDeleted(cursor.isNull(offset + 10) ? null : cursor.getShort(offset + 10) != 0);
        entity.setVersion(cursor.isNull(offset + 11) ? null : cursor.getInt(offset + 11));
        entity.setApplicationApplicationId(cursor.getLong(offset + 12));
        entity.setGender(cursor.isNull(offset + 13) ? null : Gender.fromInt(cursor.getLong(offset + 13)));
        entity.setId(cursor.isNull(offset + 14) ? null : cursor.getLong(offset + 14));
        entity.setDateCreated(cursor.isNull(offset + 15) ? null : cursor.getString(offset + 15));
        entity.setNotes(cursor.isNull(offset + 16) ? null : cursor.getString(offset + 16));
        entity.setApplicationID(cursor.isNull(offset + 17) ? null : cursor.getString(offset + 17));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ApplicantChild entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ApplicantChild entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "children" to-many relationship of Application. */
    public List<ApplicantChild> _queryApplication_Children(long childrenApplicationId) {
        synchronized (this) {
            if (application_ChildrenQuery == null) {
                QueryBuilder<ApplicantChild> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ChildrenApplicationId.eq(null));
                application_ChildrenQuery = queryBuilder.build();
            }
        }
        Query<ApplicantChild> query = application_ChildrenQuery.forCurrentThread();
        query.setParameter(0, childrenApplicationId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getApplicationDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getSaveResultDao().getAllColumns());
            builder.append(" FROM APPLICANT_CHILD T");
            builder.append(" LEFT JOIN APPLICATION T0 ON T.'APPLICATION_APPLICATION_ID'=T0.'_id'");
            builder.append(" LEFT JOIN SAVE_RESULT T1 ON T.'SAVE_RESULT_SAVE_RESULT_ID'=T1.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ApplicantChild loadCurrentDeep(Cursor cursor, boolean lock) {
        ApplicantChild entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Application application = loadCurrentOther(daoSession.getApplicationDao(), cursor, offset);
         if(application != null) {
            entity.setApplication(application);
        }
        offset += daoSession.getApplicationDao().getAllColumns().length;

        SaveResult saveResult = loadCurrentOther(daoSession.getSaveResultDao(), cursor, offset);
         if(saveResult != null) {
            entity.setSaveResult(saveResult);
        }

        return entity;    
    }

    public ApplicantChild loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ApplicantChild> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ApplicantChild> list = new ArrayList<ApplicantChild>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ApplicantChild> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ApplicantChild> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
    @Override
    protected void onPreInsertEntity(ApplicantChild entity) {
        entity.insertBase(daoSession.getSyncBaseDao());
        entity.setSyncBaseId(entity.getSyncBaseId());
    }

    @Override
    protected void onPreLoadEntity(ApplicantChild entity) {
        entity.loadBase(daoSession.getSyncBaseDao(), entity.getSyncBaseId());
    }

    @Override
    protected void onPreRefreshEntity(ApplicantChild entity) {
        entity.loadBase(daoSession.getSyncBaseDao(), entity.getSyncBaseId());
    }

    @Override
    protected void onPreUpdateEntity(ApplicantChild entity) {
        entity.updateBase(daoSession.getSyncBaseDao());
    }

    @Override
    protected void onPreDeleteEntity(ApplicantChild entity) {
        entity.deleteBase(daoSession.getSyncBaseDao());
    }

    static {
        GreenSync.registerListTypeToken("ApplicantChild", new TypeToken<List<ApplicantChild>>(){}.getType());
        GreenSync.registerTypeToken("ApplicantChild", ApplicantChild.class);
    }

}
