package com.saulpower.GreenWireTest.database;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;
import com.google.gson.reflect.TypeToken;
import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.sync.GreenSync;

import java.util.ArrayList;
import java.util.List;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table CENTER.
*/
public class CenterDao extends AbstractDao<Center, Long> {

    public static final String TABLENAME = "CENTER";

    /**
     * Properties of entity Center.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property ExternalID = new Property(0, String.class, "externalID", false, "EXTERNAL_ID");
        public final static Property EnrollmentStatus = new Property(1, EnrollmentStatus.class, "enrollmentStatus", false, "ENROLLMENT_STATUS");
        public final static Property Guid = new Property(2, String.class, "guid", false, "GUID");
        public final static Property Name = new Property(3, String.class, "name", false, "NAME");
        public final static Property ParentOUBaseId = new Property(4, long.class, "parentOUBaseId", false, "PARENT_OUBASE_ID");
        public final static Property Capacity = new Property(5, Integer.class, "capacity", false, "CAPACITY");
        public final static Property TagString = new Property(6, String.class, "tagString", false, "TAG_STRING");
        public final static Property StateTaxID = new Property(7, String.class, "stateTaxID", false, "STATE_TAX_ID");
        public final static Property TenantID = new Property(8, Long.class, "tenantID", false, "TENANT_ID");
        public final static Property SaveResultSaveResultId = new Property(9, long.class, "saveResultSaveResultId", false, "SAVE_RESULT_SAVE_RESULT_ID");
        public final static Property DateLastModified = new Property(10, String.class, "dateLastModified", false, "DATE_LAST_MODIFIED");
        public final static Property Number = new Property(11, String.class, "number", false, "NUMBER");
        public final static Property OUTypeOUTypeId = new Property(12, long.class, "oUTypeOUTypeId", false, "O_UTYPE_OUTYPE_ID");
        public final static Property SyncBaseId = new Property(13, Long.class, "syncBaseId", false, "SYNC_BASE_ID");
        public final static Property IsDeleted = new Property(14, Boolean.class, "isDeleted", false, "IS_DELETED");
        public final static Property FederalTaxID = new Property(15, String.class, "federalTaxID", false, "FEDERAL_TAX_ID");
        public final static Property Version = new Property(16, Integer.class, "version", false, "VERSION");
        public final static Property Id = new Property(17, Long.class, "id", true, "_id");
        public final static Property ParentID = new Property(18, String.class, "parentID", false, "PARENT_ID");
        public final static Property DateCreated = new Property(19, String.class, "dateCreated", false, "DATE_CREATED");
        public final static Property IsActive = new Property(20, Boolean.class, "isActive", false, "IS_ACTIVE");
    };

    private DaoSession daoSession;

    public CenterDao(DaoConfig config) {
        super(config);
    }
    
    public CenterDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'CENTER' (" + //
                "'EXTERNAL_ID' TEXT," + // 0: externalID
                "'ENROLLMENT_STATUS' INTEGER," + // 1: enrollmentStatus
                "'GUID' TEXT," + // 2: guid
                "'NAME' TEXT," + // 3: name
                "'PARENT_OUBASE_ID' INTEGER NOT NULL ," + // 4: parentOUBaseId
                "'CAPACITY' INTEGER," + // 5: capacity
                "'TAG_STRING' TEXT," + // 6: tagString
                "'STATE_TAX_ID' TEXT," + // 7: stateTaxID
                "'TENANT_ID' INTEGER," + // 8: tenantID
                "'SAVE_RESULT_SAVE_RESULT_ID' INTEGER NOT NULL ," + // 9: saveResultSaveResultId
                "'DATE_LAST_MODIFIED' TEXT," + // 10: dateLastModified
                "'NUMBER' TEXT," + // 11: number
                "'O_UTYPE_OUTYPE_ID' INTEGER NOT NULL ," + // 12: oUTypeOUTypeId
                "'SYNC_BASE_ID' INTEGER REFERENCES 'SYNC_BASE'('SYNC_BASE_ID') ," + // 13: syncBaseId
                "'IS_DELETED' INTEGER," + // 14: isDeleted
                "'FEDERAL_TAX_ID' TEXT," + // 15: federalTaxID
                "'VERSION' INTEGER," + // 16: version
                "'_id' INTEGER PRIMARY KEY ," + // 17: id
                "'PARENT_ID' TEXT," + // 18: parentID
                "'DATE_CREATED' TEXT," + // 19: dateCreated
                "'IS_ACTIVE' INTEGER);"); // 20: isActive
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'CENTER'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Center entity) {
        stmt.clearBindings();
 
        String externalID = entity.getExternalID();
        if (externalID != null) {
            stmt.bindString(1, externalID);
        }
 
        EnrollmentStatus enrollmentStatus = entity.getEnrollmentStatus();
        if (enrollmentStatus != null) {
            stmt.bindLong(2, enrollmentStatus.getValue());
        }
 
        String guid = entity.getGuid();
        if (guid != null) {
            stmt.bindString(3, guid);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(4, name);
        }
        stmt.bindLong(5, entity.getParentOUBaseId());
 
        Integer capacity = entity.getCapacity();
        if (capacity != null) {
            stmt.bindLong(6, capacity);
        }
 
        String tagString = entity.getTagString();
        if (tagString != null) {
            stmt.bindString(7, tagString);
        }
 
        String stateTaxID = entity.getStateTaxID();
        if (stateTaxID != null) {
            stmt.bindString(8, stateTaxID);
        }
 
        Long tenantID = entity.getTenantID();
        if (tenantID != null) {
            stmt.bindLong(9, tenantID);
        }
        stmt.bindLong(10, entity.getSaveResultSaveResultId());
 
        String dateLastModified = entity.getDateLastModified();
        if (dateLastModified != null) {
            stmt.bindString(11, dateLastModified);
        }
 
        String number = entity.getNumber();
        if (number != null) {
            stmt.bindString(12, number);
        }
        stmt.bindLong(13, entity.getOUTypeOUTypeId());
 
        Long syncBaseId = entity.getSyncBaseId();
        if (syncBaseId != null) {
            stmt.bindLong(14, syncBaseId);
        }
 
        Boolean isDeleted = entity.getIsDeleted();
        if (isDeleted != null) {
            stmt.bindLong(15, isDeleted ? 1l: 0l);
        }
 
        String federalTaxID = entity.getFederalTaxID();
        if (federalTaxID != null) {
            stmt.bindString(16, federalTaxID);
        }
 
        Integer version = entity.getVersion();
        if (version != null) {
            stmt.bindLong(17, version);
        }
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(18, id);
        }
 
        String parentID = entity.getParentID();
        if (parentID != null) {
            stmt.bindString(19, parentID);
        }
 
        String dateCreated = entity.getDateCreated();
        if (dateCreated != null) {
            stmt.bindString(20, dateCreated);
        }
 
        Boolean isActive = entity.getIsActive();
        if (isActive != null) {
            stmt.bindLong(21, isActive ? 1l: 0l);
        }
    }

    @Override
    protected void attachEntity(Center entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 17) ? null : cursor.getLong(offset + 17);
    }    

    /** @inheritdoc */
    @Override
    public Center readEntity(Cursor cursor, int offset) {
        Center entity = new Center( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // externalID
            cursor.isNull(offset + 1) ? null : EnrollmentStatus.fromInt(cursor.getLong(offset + 1)), // enrollmentStatus
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // guid
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // name
            cursor.getLong(offset + 4), // parentOUBaseId
            cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5), // capacity
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // tagString
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7), // stateTaxID
            cursor.isNull(offset + 8) ? null : cursor.getLong(offset + 8), // tenantID
            cursor.getLong(offset + 9), // saveResultSaveResultId
            cursor.isNull(offset + 10) ? null : cursor.getString(offset + 10), // dateLastModified
            cursor.isNull(offset + 11) ? null : cursor.getString(offset + 11), // number
            cursor.getLong(offset + 12), // oUTypeOUTypeId
            cursor.isNull(offset + 13) ? null : cursor.getLong(offset + 13), // syncBaseId
            cursor.isNull(offset + 14) ? null : cursor.getShort(offset + 14) != 0, // isDeleted
            cursor.isNull(offset + 15) ? null : cursor.getString(offset + 15), // federalTaxID
            cursor.isNull(offset + 16) ? null : cursor.getInt(offset + 16), // version
            cursor.isNull(offset + 17) ? null : cursor.getLong(offset + 17), // id
            cursor.isNull(offset + 18) ? null : cursor.getString(offset + 18), // parentID
            cursor.isNull(offset + 19) ? null : cursor.getString(offset + 19), // dateCreated
            cursor.isNull(offset + 20) ? null : cursor.getShort(offset + 20) != 0 // isActive
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Center entity, int offset) {
        entity.setExternalID(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setEnrollmentStatus(cursor.isNull(offset + 1) ? null : EnrollmentStatus.fromInt(cursor.getLong(offset + 1)));
        entity.setGuid(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setParentOUBaseId(cursor.getLong(offset + 4));
        entity.setCapacity(cursor.isNull(offset + 5) ? null : cursor.getInt(offset + 5));
        entity.setTagString(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setStateTaxID(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
        entity.setTenantID(cursor.isNull(offset + 8) ? null : cursor.getLong(offset + 8));
        entity.setSaveResultSaveResultId(cursor.getLong(offset + 9));
        entity.setDateLastModified(cursor.isNull(offset + 10) ? null : cursor.getString(offset + 10));
        entity.setNumber(cursor.isNull(offset + 11) ? null : cursor.getString(offset + 11));
        entity.setOUTypeOUTypeId(cursor.getLong(offset + 12));
        entity.setSyncBaseId(cursor.isNull(offset + 13) ? null : cursor.getLong(offset + 13));
        entity.setIsDeleted(cursor.isNull(offset + 14) ? null : cursor.getShort(offset + 14) != 0);
        entity.setFederalTaxID(cursor.isNull(offset + 15) ? null : cursor.getString(offset + 15));
        entity.setVersion(cursor.isNull(offset + 16) ? null : cursor.getInt(offset + 16));
        entity.setId(cursor.isNull(offset + 17) ? null : cursor.getLong(offset + 17));
        entity.setParentID(cursor.isNull(offset + 18) ? null : cursor.getString(offset + 18));
        entity.setDateCreated(cursor.isNull(offset + 19) ? null : cursor.getString(offset + 19));
        entity.setIsActive(cursor.isNull(offset + 20) ? null : cursor.getShort(offset + 20) != 0);
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Center entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Center entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getOUTypeDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getOUBaseDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getSaveResultDao().getAllColumns());
            builder.append(" FROM CENTER T");
            builder.append(" LEFT JOIN OUTYPE T0 ON T.'O_UTYPE_OUTYPE_ID'=T0.'_id'");
            builder.append(" LEFT JOIN OUBASE T1 ON T.'PARENT_OUBASE_ID'=T1.'_id'");
            builder.append(" LEFT JOIN SAVE_RESULT T2 ON T.'SAVE_RESULT_SAVE_RESULT_ID'=T2.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Center loadCurrentDeep(Cursor cursor, boolean lock) {
        Center entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        OUType oUType = loadCurrentOther(daoSession.getOUTypeDao(), cursor, offset);
         if(oUType != null) {
            entity.setOUType(oUType);
        }
        offset += daoSession.getOUTypeDao().getAllColumns().length;

        OUBase parent = loadCurrentOther(daoSession.getOUBaseDao(), cursor, offset);
         if(parent != null) {
            entity.setParent(parent);
        }
        offset += daoSession.getOUBaseDao().getAllColumns().length;

        SaveResult saveResult = loadCurrentOther(daoSession.getSaveResultDao(), cursor, offset);
         if(saveResult != null) {
            entity.setSaveResult(saveResult);
        }

        return entity;    
    }

    public Center loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Center> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Center> list = new ArrayList<Center>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Center> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Center> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
    @Override
    protected void onPreInsertEntity(Center entity) {
        entity.insertBase(daoSession.getSyncBaseDao());
        entity.setSyncBaseId(entity.getSyncBaseId());
    }

    @Override
    protected void onPreLoadEntity(Center entity) {
        entity.loadBase(daoSession.getSyncBaseDao(), entity.getSyncBaseId());
    }

    @Override
    protected void onPreRefreshEntity(Center entity) {
        entity.loadBase(daoSession.getSyncBaseDao(), entity.getSyncBaseId());
    }

    @Override
    protected void onPreUpdateEntity(Center entity) {
        entity.updateBase(daoSession.getSyncBaseDao());
    }

    @Override
    protected void onPreDeleteEntity(Center entity) {
        entity.deleteBase(daoSession.getSyncBaseDao());
    }

    static {
        GreenSync.registerListTypeToken("Center", new TypeToken<List<Center>>(){}.getType());
        GreenSync.registerTypeToken("Center", Center.class);
    }

}
