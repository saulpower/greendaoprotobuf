package com.saulpower.GreenWireTest.database;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import com.saulpower.GreenWireTest.database.ClassAssignment;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table CLASS_ASSIGNMENT.
*/
public class ClassAssignmentDao extends AbstractDao<ClassAssignment, Long> {

    public static final String TABLENAME = "CLASS_ASSIGNMENT";

    /**
     * Properties of entity ClassAssignment.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property EmployeesClaszId = new Property(0, long.class, "employeesClaszId", false, "EMPLOYEES_CLASZ_ID");
        public final static Property PersonPersonId = new Property(1, long.class, "personPersonId", false, "PERSON_PERSON_ID");
        public final static Property IsPrimary = new Property(2, Boolean.class, "isPrimary", false, "IS_PRIMARY");
        public final static Property ExternalID = new Property(3, String.class, "externalID", false, "EXTERNAL_ID");
        public final static Property Guid = new Property(4, String.class, "guid", false, "GUID");
        public final static Property Name = new Property(5, String.class, "name", false, "NAME");
        public final static Property ClaszClaszId = new Property(6, long.class, "claszClaszId", false, "CLASZ_CLASZ_ID");
        public final static Property ClassID = new Property(7, String.class, "classID", false, "CLASS_ID");
        public final static Property TagString = new Property(8, String.class, "tagString", false, "TAG_STRING");
        public final static Property TenantID = new Property(9, Long.class, "tenantID", false, "TENANT_ID");
        public final static Property SaveResultSaveResultId = new Property(10, long.class, "saveResultSaveResultId", false, "SAVE_RESULT_SAVE_RESULT_ID");
        public final static Property DateLastModified = new Property(11, Long.class, "dateLastModified", false, "DATE_LAST_MODIFIED");
        public final static Property ClassAssignmentsStudentId = new Property(12, long.class, "classAssignmentsStudentId", false, "CLASS_ASSIGNMENTS_STUDENT_ID");
        public final static Property IsDeleted = new Property(13, Boolean.class, "isDeleted", false, "IS_DELETED");
        public final static Property Version = new Property(14, Integer.class, "version", false, "VERSION");
        public final static Property Id = new Property(15, Long.class, "id", true, "_id");
        public final static Property DateCreated = new Property(16, Long.class, "dateCreated", false, "DATE_CREATED");
        public final static Property ClassAssignmentsClaszId = new Property(17, long.class, "classAssignmentsClaszId", false, "CLASS_ASSIGNMENTS_CLASZ_ID");
    };

    private DaoSession daoSession;

    private Query<ClassAssignment> student_ClassAssignmentsQuery;
    private Query<ClassAssignment> clasz_ClassAssignmentsQuery;
    private Query<ClassAssignment> clasz_EmployeesQuery;

    public ClassAssignmentDao(DaoConfig config) {
        super(config);
    }
    
    public ClassAssignmentDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'CLASS_ASSIGNMENT' (" + //
                "'EMPLOYEES_CLASZ_ID' INTEGER NOT NULL ," + // 0: employeesClaszId
                "'PERSON_PERSON_ID' INTEGER NOT NULL ," + // 1: personPersonId
                "'IS_PRIMARY' INTEGER," + // 2: isPrimary
                "'EXTERNAL_ID' TEXT," + // 3: externalID
                "'GUID' TEXT," + // 4: guid
                "'NAME' TEXT," + // 5: name
                "'CLASZ_CLASZ_ID' INTEGER NOT NULL ," + // 6: claszClaszId
                "'CLASS_ID' TEXT," + // 7: classID
                "'TAG_STRING' TEXT," + // 8: tagString
                "'TENANT_ID' INTEGER," + // 9: tenantID
                "'SAVE_RESULT_SAVE_RESULT_ID' INTEGER NOT NULL ," + // 10: saveResultSaveResultId
                "'DATE_LAST_MODIFIED' INTEGER," + // 11: dateLastModified
                "'CLASS_ASSIGNMENTS_STUDENT_ID' INTEGER NOT NULL ," + // 12: classAssignmentsStudentId
                "'IS_DELETED' INTEGER," + // 13: isDeleted
                "'VERSION' INTEGER," + // 14: version
                "'_id' INTEGER PRIMARY KEY ," + // 15: id
                "'DATE_CREATED' INTEGER," + // 16: dateCreated
                "'CLASS_ASSIGNMENTS_CLASZ_ID' INTEGER NOT NULL );"); // 17: classAssignmentsClaszId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'CLASS_ASSIGNMENT'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, ClassAssignment entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getEmployeesClaszId());
        stmt.bindLong(2, entity.getPersonPersonId());
 
        Boolean isPrimary = entity.getIsPrimary();
        if (isPrimary != null) {
            stmt.bindLong(3, isPrimary ? 1l: 0l);
        }
 
        String externalID = entity.getExternalID();
        if (externalID != null) {
            stmt.bindString(4, externalID);
        }
 
        String guid = entity.getGuid();
        if (guid != null) {
            stmt.bindString(5, guid);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(6, name);
        }
        stmt.bindLong(7, entity.getClaszClaszId());
 
        String classID = entity.getClassID();
        if (classID != null) {
            stmt.bindString(8, classID);
        }
 
        String tagString = entity.getTagString();
        if (tagString != null) {
            stmt.bindString(9, tagString);
        }
 
        Long tenantID = entity.getTenantID();
        if (tenantID != null) {
            stmt.bindLong(10, tenantID);
        }
        stmt.bindLong(11, entity.getSaveResultSaveResultId());
 
        Long dateLastModified = entity.getDateLastModified();
        if (dateLastModified != null) {
            stmt.bindLong(12, dateLastModified);
        }
        stmt.bindLong(13, entity.getClassAssignmentsStudentId());
 
        Boolean isDeleted = entity.getIsDeleted();
        if (isDeleted != null) {
            stmt.bindLong(14, isDeleted ? 1l: 0l);
        }
 
        Integer version = entity.getVersion();
        if (version != null) {
            stmt.bindLong(15, version);
        }
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(16, id);
        }
 
        Long dateCreated = entity.getDateCreated();
        if (dateCreated != null) {
            stmt.bindLong(17, dateCreated);
        }
        stmt.bindLong(18, entity.getClassAssignmentsClaszId());
    }

    @Override
    protected void attachEntity(ClassAssignment entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 15) ? null : cursor.getLong(offset + 15);
    }    

    /** @inheritdoc */
    @Override
    public ClassAssignment readEntity(Cursor cursor, int offset) {
        ClassAssignment entity = new ClassAssignment( //
            cursor.getLong(offset + 0), // employeesClaszId
            cursor.getLong(offset + 1), // personPersonId
            cursor.isNull(offset + 2) ? null : cursor.getShort(offset + 2) != 0, // isPrimary
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // externalID
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // guid
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // name
            cursor.getLong(offset + 6), // claszClaszId
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7), // classID
            cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8), // tagString
            cursor.isNull(offset + 9) ? null : cursor.getLong(offset + 9), // tenantID
            cursor.getLong(offset + 10), // saveResultSaveResultId
            cursor.isNull(offset + 11) ? null : cursor.getLong(offset + 11), // dateLastModified
            cursor.getLong(offset + 12), // classAssignmentsStudentId
            cursor.isNull(offset + 13) ? null : cursor.getShort(offset + 13) != 0, // isDeleted
            cursor.isNull(offset + 14) ? null : cursor.getInt(offset + 14), // version
            cursor.isNull(offset + 15) ? null : cursor.getLong(offset + 15), // id
            cursor.isNull(offset + 16) ? null : cursor.getLong(offset + 16), // dateCreated
            cursor.getLong(offset + 17) // classAssignmentsClaszId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, ClassAssignment entity, int offset) {
        entity.setEmployeesClaszId(cursor.getLong(offset + 0));
        entity.setPersonPersonId(cursor.getLong(offset + 1));
        entity.setIsPrimary(cursor.isNull(offset + 2) ? null : cursor.getShort(offset + 2) != 0);
        entity.setExternalID(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setGuid(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setName(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setClaszClaszId(cursor.getLong(offset + 6));
        entity.setClassID(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
        entity.setTagString(cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8));
        entity.setTenantID(cursor.isNull(offset + 9) ? null : cursor.getLong(offset + 9));
        entity.setSaveResultSaveResultId(cursor.getLong(offset + 10));
        entity.setDateLastModified(cursor.isNull(offset + 11) ? null : cursor.getLong(offset + 11));
        entity.setClassAssignmentsStudentId(cursor.getLong(offset + 12));
        entity.setIsDeleted(cursor.isNull(offset + 13) ? null : cursor.getShort(offset + 13) != 0);
        entity.setVersion(cursor.isNull(offset + 14) ? null : cursor.getInt(offset + 14));
        entity.setId(cursor.isNull(offset + 15) ? null : cursor.getLong(offset + 15));
        entity.setDateCreated(cursor.isNull(offset + 16) ? null : cursor.getLong(offset + 16));
        entity.setClassAssignmentsClaszId(cursor.getLong(offset + 17));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(ClassAssignment entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(ClassAssignment entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "classAssignments" to-many relationship of Student. */
    public List<ClassAssignment> _queryStudent_ClassAssignments(long classAssignmentsStudentId) {
        synchronized (this) {
            if (student_ClassAssignmentsQuery == null) {
                QueryBuilder<ClassAssignment> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ClassAssignmentsStudentId.eq(null));
                student_ClassAssignmentsQuery = queryBuilder.build();
            }
        }
        Query<ClassAssignment> query = student_ClassAssignmentsQuery.forCurrentThread();
        query.setParameter(0, classAssignmentsStudentId);
        return query.list();
    }

    /** Internal query to resolve the "classAssignments" to-many relationship of Clasz. */
    public List<ClassAssignment> _queryClasz_ClassAssignments(long classAssignmentsClaszId) {
        synchronized (this) {
            if (clasz_ClassAssignmentsQuery == null) {
                QueryBuilder<ClassAssignment> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ClassAssignmentsClaszId.eq(null));
                clasz_ClassAssignmentsQuery = queryBuilder.build();
            }
        }
        Query<ClassAssignment> query = clasz_ClassAssignmentsQuery.forCurrentThread();
        query.setParameter(0, classAssignmentsClaszId);
        return query.list();
    }

    /** Internal query to resolve the "employees" to-many relationship of Clasz. */
    public List<ClassAssignment> _queryClasz_Employees(long employeesClaszId) {
        synchronized (this) {
            if (clasz_EmployeesQuery == null) {
                QueryBuilder<ClassAssignment> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.EmployeesClaszId.eq(null));
                clasz_EmployeesQuery = queryBuilder.build();
            }
        }
        Query<ClassAssignment> query = clasz_EmployeesQuery.forCurrentThread();
        query.setParameter(0, employeesClaszId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPersonDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getClaszDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getSaveResultDao().getAllColumns());
            builder.append(" FROM CLASS_ASSIGNMENT T");
            builder.append(" LEFT JOIN PERSON T0 ON T.'PERSON_PERSON_ID'=T0.'_id'");
            builder.append(" LEFT JOIN CLASZ T1 ON T.'CLASZ_CLASZ_ID'=T1.'_id'");
            builder.append(" LEFT JOIN SAVE_RESULT T2 ON T.'SAVE_RESULT_SAVE_RESULT_ID'=T2.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ClassAssignment loadCurrentDeep(Cursor cursor, boolean lock) {
        ClassAssignment entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Person person = loadCurrentOther(daoSession.getPersonDao(), cursor, offset);
         if(person != null) {
            entity.setPerson(person);
        }
        offset += daoSession.getPersonDao().getAllColumns().length;

        Clasz clasz = loadCurrentOther(daoSession.getClaszDao(), cursor, offset);
         if(clasz != null) {
            entity.setClasz(clasz);
        }
        offset += daoSession.getClaszDao().getAllColumns().length;

        SaveResult saveResult = loadCurrentOther(daoSession.getSaveResultDao(), cursor, offset);
         if(saveResult != null) {
            entity.setSaveResult(saveResult);
        }

        return entity;    
    }

    public ClassAssignment loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ClassAssignment> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ClassAssignment> list = new ArrayList<ClassAssignment>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ClassAssignment> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ClassAssignment> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
